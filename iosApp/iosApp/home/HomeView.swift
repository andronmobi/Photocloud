import SwiftUI
import sharedClient

struct HomeView: View {
    var body: some View {
        Text("Home Screen")
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView()
    }
}
